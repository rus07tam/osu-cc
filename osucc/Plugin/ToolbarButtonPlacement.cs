namespace osucc.Plugin
{
    /// <summary>Which side of the toolbar a plugin button is placed on.</summary>
    public enum ToolbarButtonPlacement
    {
        /// <summary>Left-hand group (next to the settings and home buttons).</summary>
        Left,

        /// <summary>Right-hand group (next to news, wiki, user, ...).</summary>
        Right,
    }
}
