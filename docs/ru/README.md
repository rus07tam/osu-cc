# osu!cc

**Документация:** [English](../en/README.md) · Русский · [Разработка](DEVELOPMENT.md) · [Безопасность](SECURITY.md) · [Контрибуции](CONTRIBUTING.md) · [Кодекс поведения](../../CODE_OF_CONDUCT.md)

osu!cc это дополнение к osu!lazer. Это не официальный клиент, а набор опциональных
улучшений поверх игры, которая у вас уже стоит.

Установка osu! при этом не трогается: в папку игры ничего не копируется и не
изменяется, а x-token подпись работает ровно как на оригинальном клиенте.
Единственное, что остаётся после нас, это собственная папка `osu-cc` в каталоге
данных игры, где лежат настройки, плагины и логи. Удалить клиент так же просто,
как удалить эту папку.

> **Внимание.** osu!cc это не официальный клиент osu!. Его использование
> нарушает Пользовательское соглашение osu!, и за свой аккаунт вы отвечаете сами.
> Мы не несём ответственности за всё, что с ним случится. И ещё кое-что: в этом
> проекте никогда не будет читов или чего-то подобного. Прежде чем пользоваться,
> прочтите [SECURITY.md](SECURITY.md).

## Скриншоты

![Менеджер плагинов](../assets/PLUGIN_MANAGER.png)

![Секция настроек и мод-селектор](../assets/SETTINGS_AND_MOD_PANEL.png)

![Целебрация нового рекорда](../assets/NEW_SCORE_CELEBRATION.png)

![Фейковый и расширенный саппортер-тег](../assets/FAKE_AND_EXTENDED_OSU_SUPPORTER.png)

![Градиентные ники в song select (лидерборд и карточка профиля)](../assets/GRADIENT_USERNAME.png)

## Что умеет

Большинство возможностей это тумблеры в отдельной секции **Specials**
в настройках игры.

### Основные возможности

- **Менеджер плагинов**: плагины могут добавлять кнопки на тулбар, секции
  настроек, оверлеи и свои уведомления. Плагины поддерживают lifecycle-хуки
  (срабатывают при установке, обновлении или удалении) и версионированные
  миграции данных, так что после обновлений они остаются в актуальном состоянии.
  Включение/выключение настраивается в менеджере плагинов.
- **Несовместимые моды**: можно выбирать и играть с модами, которые обычно
  конфликтуют друг с другом.
- **Кнопка "Random mods"**: кнопка в футере мод-селектора, которая выбирает
  случайный набор валидных модов.
- **Колонка System-модов**: обычно скрытая колонка `System` (score v2, touch
  device и т.п.) появляется в мод-селекторе.
- **Целебрация нового рекорда**: полноэкранная анимация с частицами, когда вы
  ставите новый личный рекорд.
- **Отключение сабмита скоров**: блокирует отправку соло-скоров на серверы osu!.
  Локальные скоры сохраняются, при старте плея показывается напоминание.
- **Фейковый osu! supporter**: локальный (только для вас) саппортер-тег с
  настраиваемым уровнем (от 1 до 10 сердечек) у текущего игрока (везде, где показывается
  профиль). На серверы ничего не отправляется.
- **Подсветка избранного**: избранные карты получают розовую пульсирующую
  обводку в song select.
- **Скачать всё избранное**: кнопка в секции Beatmaps → Favourites профиля
  игрока, которая скачивает все избранные карты.
- **Брендинг**: заголовок окна становится "osu!cc".
- **Стартовый тост + дисклеймер**: уведомление о том, что клиент загрузился,
  и одноразовое предупреждение.

### Встроенные плагины

Эти плагины поставляются вместе с клиентом и отключаются в менеджере плагинов:

- **Градиентные ники**: красит ники игроков горизонтальным градиентом (свой ник
  и чужие, разными палитрами). Работает в таблицах скоров, чате, профилях,
  мультиплеере и на тулбаре. Свой ник также можно заменить произвольным текстом
  или скрыть за белым блоком.
- **Subdivide Nations**: показывает субнациональный регион игрока на профилях
  и карточках.
- **oii**: показывает improvement indicator рядом с общим временем игры
  в профиле игрока.
- **Friends leaderboard**: показывает дружеский лидерборд без osu!supporter,
  собирая его на клиенте из лучшего скора каждого друга на карте.
- **Debug overlay**: тестовые панели для систем уведомлений и целебраций.
- **ExamplePlugin**: эталонная реализация для разработчиков, демонстрирующая
  API плагинов. Если не нужен, удалите.

В планах:

- webview-браузер
- браузер и апдейтер плагинов: находить плагины и их метаданные на GitHub,
  устанавливать и обновлять их из менеджера плагинов.
- больше conditional-условий для цветов ника: ручные для игроков, osu!supporter,
  наличие бейджа X, друг
- шрифт ника (тоже по условиям, как цвета)
- шрифт игры
- локальные переопределения баннеров и бейджей
- кастомные баннеры и бейджи

## Как это работает

osu!lazer это .NET-приложение, поэтому osu!cc подключается через .NET startup
hook: игра выполняет наш код ещё до того, как дойдёт до `Main()`. Это раньше, чем
обычный трюк с инъекцией правилсетов, и позволяет менять то, что другие подходы
не могут. Проприетарные `osu.Game.dll`, `osu.Game.Auth.dll` и `AuthNative.dll`
никогда не изменяются.

Хук активируется только тогда, когда osu! запущен через `osucc`. Любой другой
запуск это обычный osu!.

## Установка

### Из исходников (рекомендуется)

Понадобится .NET SDK 8.0 (скачайте его с [официального сайта](https://dotnet.microsoft.com/en-us/download/dotnet/8.0)) и установленный osu!lazer.

#### Windows

Лаунчер сам находит osu! в `%LOCALAPPDATA%\osulazer\current`.

```shell
git clone https://github.com/rus07tam/osu-cc.git
dotnet build osucc\osucc.csproj -c Debug
dotnet osucc\bin\Debug\net8.0\osucc.dll start
```

Первый `osucc start` собирает хук и плагины, деплоит их и запускает osu!.

#### Linux

Каждый дистрибутив кладёт osu! в своё место, поэтому лаунчер сначала ищет его в
`$PATH` (это покрывает nixpkgs/NixOS, пакеты из AUR и большинство ручных
установок), а потом в нескольких стандартных местах. Если у вас он лежит
в нестандартном месте, укажите путь через `--osu-dir`.

```shell
git clone https://github.com/rus07tam/osu-cc.git
dotnet build osucc/osucc.csproj -c Debug
dotnet osucc/bin/Debug/net8.0/osucc.dll start
```

Первый `osucc start` собирает хук и плагины, деплоит их и запускает osu!.

Чтобы обновить всё, подтяните свежие изменения и снова выполните `osucc start`:
`git pull` пересоберёт хук и плагины, а `osucc start` заново их задеплоит и
запустит игру.

### Бинарники (standalone, без сборки)

Чекаут и .NET SDK не нужны: лаунчер это self-contained бинарник, который сам
подтягивает готовые хук и плагины. Доступно после первого публичного релиза
(`v1.0.0`).

1. Скачайте `osucc` (Linux) или `osucc.exe` (Windows) из последнего
   [GitHub release](https://github.com/rus07tam/osu-cc/releases) и положите куда-нибудь
   в `PATH` (`chmod +x osucc` на Linux).
2. Подтяните хук и встроенные плагины:

   ```shell
   osucc update
   ```

   Это забирает `osucc.dll` и его рантайм-зависимости с nuget.org и кладёт
   архивы плагинов из того же релиза в папку данных; игра распакует их при
   следующем запуске.
3. Запустите игру:

   ```shell
   osucc run
   ```

Запускайте `osucc update` снова, когда захотите свежие хук и плагины;
`osucc update --launcher` заодно заменит сам бинарник лаунчера.

## Команды

Голый `osucc` печатает справку; каждое действие — отдельная подкоманда:

| Команда        | Что делает                                |
| ---            | ---                                       |
| `osucc build`  | собирает хук и плагины                    |
| `osucc deploy` | копирует хук и плагины в папку данных     |
| `osucc run`    | запуск osu! с уже развёрнутым хуком       |
| `osucc start`  | build + deploy + run                      |
| `osucc update` | подтягивает свежие хук + плагины (добавьте `--launcher`, чтобы обновить и сам osucc) |
| `osucc clean`  | удаляет файлы хука                        |
| `osucc status` | показывает, где что лежит                 |

Опции: `--osu-dir <путь>`, `--repo <путь>`, `-c|--config <Debug|Release>`, `--no-build`.

## Разработка

См. [docs/ru/DEVELOPMENT.md](DEVELOPMENT.md): структура репозитория, как
писать плагины и как дебажить. Хотите помочь? См. [CONTRIBUTING.md](CONTRIBUTING.md).

## Контрибьюторы

- [rus07tam](https://t.me/rus07tam_vf): создатель и сопровождающий проекта (<rus07tam@ruject.fun>).
- [Meyblow](https://t.me/Meyblow): постоянный тестер на Windows 11; придумал идеи ряда фич.
- [tryhxo](https://t.me/tryhxo): тестировал на Windows 11.

## Благодарности

- [ppy/osu](https://github.com/ppy/osu) и
  [ppy/osu-framework](https://github.com/ppy/osu-framework): игра и фреймворк,
  на которых всё построено.
- [LazerAuthlibInjection](https://github.com/MingxuanGame/LazerAuthlibInjection):
  источник подхода с хукингом.
- [oii](https://github.com/ferryhmm/oii): идея improvement indicator, лежащая
  в основе встроенного плагина `oii`.
- [osu-subdivide-nations](https://github.com/Cavitedev/osu-subdivide-nations):
  региональные флаги, вдохновившие встроенный плагин `Subdivide Nations`.
- [Harmony](https://github.com/pardeike/Harmony): библиотека патчинга, с помощью
  которой игра изменяется в рантайме.
- [SharpCompress](https://github.com/adamhathcock/sharpcompress): используется
  для распаковки архивов плагинов.

## Лицензия

См. [LICENSE](../../LICENSE).
